import { ROUTE_URL_AUTH as AUTH } from '@/pages/Auth';
import { ROUTE_URL_HOME as HOME } from '@/pages/Home';
import { ROUTE_URL_MAPS as Maps } from '@/pages/Maps';

const ROUTE_URLS = {
  HOME,
  AUTH,
  Maps,
};

export default ROUTE_URLS;
